apiVersion: v1
kind: Service
metadata:
  name: ghost-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: ghost
  ports:
  - protocol: TCP
    port: {{ ghost_port }}
    targetPort: {{ ghost_port }}
