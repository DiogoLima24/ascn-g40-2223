apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: database
  ports:
  - protocol: TCP
    port: {{ database_port }}
    targetPort: {{ database_port }}
